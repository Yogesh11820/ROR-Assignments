<h1>New Article</h1>
<%= form_with(model: @article, url: articles_path) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :body %>
  <%= form.text_area :body %>

  <%= form.submit 'Create Article' %>
<% end %>
